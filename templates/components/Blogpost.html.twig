<div{{ attributes }}>
    <div class="card m-4">
        <div class="card-body">
            <h4 class="card-title">{{ this.blogpost.title }}</h4>
            <p class="card-text">{{ this.blogpost.content }}</p>
        </div>
    </div>
</div>
